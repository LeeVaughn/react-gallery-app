import React from "react";

// returns message that no images have been found
const NotFound = () => (
    <li className="not-found">
        <h2>No Results Found</h2>
        <p>Your search did not return any results. Please try again.</p>
    </li>
);

export default NotFound;
