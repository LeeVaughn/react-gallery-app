import React from "react";

// displays message when user tries to access a URL that doesn't exist
const Error = () => (
    <div className="error">
        <h2>Page Not Found</h2>
    </div>
);

export default Error;
